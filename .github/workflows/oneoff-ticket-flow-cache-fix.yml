name: One-off ticket flow maintenance

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ticket flow cache and history maintenance has already been applied."
