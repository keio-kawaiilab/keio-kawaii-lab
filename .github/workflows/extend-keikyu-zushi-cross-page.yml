name: DISABLED - Keikyu Zushi unbanded cross-page proof

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated: the old cross-page graph referenced unsafe page+column fragments. Use section-aware fragments and section-chain evidence instead."
