name: DISABLED - Keikyu Zushi unbanded schedule-all proof

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated: page+column identity was unsafe because a PDF page can contain multiple timetable sections. Use the section-aware proof pipeline instead."
